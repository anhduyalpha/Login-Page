Four independent, single-purpose Node ESM scripts with no shared library — each is an executable entry point launched directly via `node`. Two scripts (`capture_screenshots.js`, `verify_strict_firebase.js`) use Puppeteer to automate a full register→login→profile flow on `http://localhost:3000`, while `capture_login_error.js` focuses only on the invalid-credentials error state. `convert_avatar.js` is a pure file-system transformer that reads `public/avartar.png`, embeds it as a base64 `<image>` inside an SVG with a circular clip-path, and writes both `avatar.svg` and the misspelled `avartar.svg` back to `public/`. All scripts resolve their output directory relative to `__dirname` (e.g. `../docs/screenshots`) so they can be invoked from any working directory. There are no imports between sibling scripts; cross-cutting concerns like browser launch args and viewport setup are duplicated per script rather than factored out.